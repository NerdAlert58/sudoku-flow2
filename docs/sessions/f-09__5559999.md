# Session: F-09
Date: 2026-08-06T19:20:00-05:00
Agent: subagent (test-author: general-purpose; builder: agents:golanger; 1 fix round)
Piece / Brief: F-09 (docs/features/09-catalog.md)
Baseline SHA -> Head: 5559999 -> (merge commit of feature/f-09)

## Accomplished
- catalog package: embedded puzzles.txt copy (byte-identical, cmp-verified), ordinal
  section mapping to the four canonical names, parseCatalog error contract,
  panic-at-first-use via sync.OnceValue + mustParse seam, Sections()/Raw() API,
  drift-guard test.

## Decisions made (and why)
- sync.OnceValue over init(): panic lands at first use, parseCatalog stays a clean
  testable seam.
- Hand-rolled 81-digit byte check over regexp: same contract, less machinery; byte loop
  kept over range (ASCII precision) — leanness idiom nudge rejected as out-of-lane.
- mustParse extraction (fix round): separates fallible parser from infallible embed
  invariant; test-verifier-mandated.

## Deviations from the frozen plan
- none

## Test evidence
- Red-state (5.0): compile failure (undefined parseCatalog/Sections) — correct red.
  Fix-round red: undefined mustParse.
- Green-state (5.1): go test -race -count=1 ./... green repo-wide, twice.
- Coverage: 88.4% total; after fix round parseCatalog/mustParse at 100%; max-aggregated
  profile shows zero uncovered catalog blocks.
- Test-exempt lines applied: none.
- test-verifier verdict: round 1 FAIL (2 blocking: panic branch + orphan-puzzle path
  uncovered) → routed per 5b.0 (test-author added 2 tests; builder extracted mustParse)
  → round 2 PASS (tuned; mutation-killed; byte-identical restore). RUBRIC_GAPs: RG-1
  single-mutant sampling (advisory), RG-2 untracked-tree attribution (process note:
  consider WIP commits before builder dispatch on future pieces).
- Compliance evidence: (no compliance ACs — hats N/A)

## Deployment evidence
- **Target:** manual (cicd_deploy_hook: manual)
- deployment: SKIP (cicd_deploy_hook: manual)
- **Rollback command (unrun):** N/A — library piece

## Leanness review
- **RIGOR:** basic
- **Findings:** 1 shrink (inline isPuzzleLine) — REJECTED as out-of-lane per role card
  ("never proposes inlining a named function"; decomposition is readability's surface,
  which passed it). Re-run after fix round: same conclusion, Lean already. Ship.
- **Net removable:** 0 accepted
- **Disposition:** advisory-only

## Readability review
- **Mode:** blocking · **RIGOR:** basic
- **Findings:** Readable. Ship. (both rounds; post-fix re-run: mustParse extraction
  judged justified, worst function 24/50 lines, cyclomatic 8/10, nesting 3/3)
- **Unauthorized budget breaches:** 0 · **Authorized exemptions:** none
- **Disposition:** VERDICT: PASS both rounds

## Open / next session
- F-10 wires GET /v1/puzzles to catalog.Sections() (wire shape already pinned by
  TestSectionJSONWireShape).
