# Session: F-10
Date: 2026-08-07T04:30:00-05:00
Agent: subagent (test-author: general-purpose + 1 conditioned pin round; builder: agents:golanger)
Piece / Brief: F-10 (docs/features/10-http-contract.md)
Baseline SHA -> Head: 23c93f3 (pre-F-08-merge; piece diff = branch commits vs origin/master post-rebase)

## Accomplished
- Full /v1 surface: solve (solve-shape 400 for domain-invalid; float ms; events never
  null), generate (5s ctx; mapGenerateError seam; difficulty==grade==canonical band),
  validate-batch (wg.Go fan-out, pre-sized slice, ADR-0014 item values, 257→413
  post-parse pre-solve), puzzles (byte-equal catalog marshal), shared transport gate
  (405→415→413 precedence; Content-Length fast-path with ZERO body reads + lazy
  MaxBytesReader).
- 55-corpus contract test through the real handler, oracle-equal, 0.22s.
- Verifier-conditioned pin added same-branch: stalled/unsolvable are 200 with partial
  grid solution, grade "", iterations==eventCount+1 — no production change needed.

## Decisions made (and why)
- Interpretive freeze (coordinator-ratified): generate response difficulty == grade ==
  canonical band string — the only reading making ADR-0009's equality literal.
- Non-solved solution = partial grid — verifier UPHELD against PRD honesty clause
  (constructively proven placements + zeros; neither guessed nor fabricable as
  complete); now wire-pinned.
- ladderRank mirror in batch.go: solver exports no ranking surface; mandated by
  containment; drift-pinned by corpus test.

## Deviations from the frozen plan
- F-08 sequencing slip: f-10 branched before PR#13 merged (watcher loop exhausted);
  builder materialized generate/ from the branch to compile; resolved by pre-review
  commit + rebase onto master post-merge. Process note: watchers must assert MERGED,
  not exit on loop end (D-030).

## Test evidence
- Red-state: 19 tests red honestly (404s on absent routes) + seam compile-red; 2
  green-by-design F-01 behavior pins.
- Green-state: repo-wide -race -count=1 green (author, builder, verifier, pin round).
- test-verifier: PASS (tuned; 5/5 mutations killed incl. read-tracker fast-path proof,
  405-beats-415, batch order-under-race, envelope-vs-solve-shape, events-null; role
  separation structural; ruling upheld with pin condition — DISCHARGED same branch).
- RUBRIC_GAPs → FOLLOWUPS: wire-level generation_failed body untestable by design
  (seam-tested); AC-2 instrument is DeepEqual-minus-solveTimeMs (deterministic-marshal
  equivalence).
- Compliance evidence: (no compliance ACs — hats N/A)

## Deployment evidence
- **Target:** manual (cicd_deploy_hook: manual) — deployment: SKIP
- **Rollback command (unrun):** (inherit) — exercised at F-13

## Leanness review
- **RIGOR:** basic · **Findings:** 1 advisory (newRand crypto-seeding → one-liner) →
  FOLLOWUPS · **Disposition:** advisory-only ("Lean already. Ship." otherwise)

## Readability review
- **Mode:** blocking · **RIGOR:** basic · **Findings:** Readable. Ship. (worst fn
  34/50, file 101/400, nesting 2/3, cyclomatic ~7/10) · **Disposition:** VERDICT: PASS

## Open / next session
- F-11 consumes the now-pinned wire (incl. stalled partial-grid display).
- F-13 smoke covers the deployed wire-500 surface indirectly via headers assertions.
