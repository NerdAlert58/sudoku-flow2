# Session: F-01
Date: 2026-08-06T18:30:00-05:00
Agent: subagent (test-author: general-purpose; builder: agents:golanger)
Piece / Brief: F-01 (docs/features/01-walking-skeleton.md)
Baseline SHA -> Head: 6399b58 -> (merge commit of feature/f-01)

## Accomplished
- Module scaffold (go.mod pinned go1.24.1, zero requires, no go.sum, no internal/),
  httpapi shell with the frozen five-stage middleware chain, /v1/health, envelope
  404/405/500, embedded placeholder SPA, both entrypoints, vercel.json (classic model,
  maxDuration 10).
- Live Vercel PREVIEW deploy spike succeeded; deployed health + frozen headers verified.
  Platform findings promoted to ADR-0016.
- Coverage 85.4% (≥80 floor) via -coverpkg; LCOV produced.

## Decisions made (and why)
- framework:null in vercel.json — the new Go preset wins auto-detection and breaks the
  classic functions block; explicit opt-out restores the PRD-proven model (ADR-0016).
- maxDuration:10 — smallest standard value covering the 5s generate deadline
  (SECURITY F-10 satisfied on the configure branch).
- cors middleware ships as real empty-allowlist middleware per frozen chain contract
  (ARCHITECTURE §Summary, AUDIT S2) despite zero reachable branch today.
- HEAD → 405 on /v1/health — documented ADR-0005 consequence.

## Deviations from the frozen plan
- None in code. Process: bare `vercel deploy` targets PRODUCTION on git-less projects
  (CLI 58.7.1) — spike used explicit --target=preview; F-02's workflow must always pass
  explicit targets (carried into ADR-0016).

## Test evidence
- Red-state capture (Phase 5.0): module-level failure (no go.mod — declared-valid red
  per D-027); 5 test files, AC map in test manifest.
- Green-state capture (Phase 5.1): go test -race ./... ok; no test modified/deleted
  (mutation-verified intact by test-verifier).
- Coverage report: coverage.lcov (lcov), 85.4% overall; uncovered = exempted entrypoints
  + CORS echo branch ruled EXEMPT unreachable-by-design with CONDITION: the piece that
  first populates the allowlist must add exact-match-echo + Vary: Origin tests.
  (Provenance note: coverage.lcov produced by the coordinator at 5b.6 with freshly
  installed gcov2lcov@latest — resolving the builder's "not installed" note.)
- Test-exempt lines applied: 2 (api/index.go, cmd/server/main.go — thin entrypoints,
  live-deploy exercised).
- test-verifier verdict: PASS (tuned; 9/9 ACs; 3 mutations killed; 0 blocking).
- Compliance evidence: (no compliance ACs — hats N/A)

## Deployment evidence
- **Target:** manual (cicd_deploy_hook: manual — CI-managed via gated workflow)
- deployment: SKIP (cicd_deploy_hook: manual). The piece's own AC-6 preview spike ran
  live: https://sudoku-flow2-jkbpfjgfg-nerdalert58s-projects.vercel.app (preview,
  SSO-protected; verified via vercel curl).
- **Rollback command (unrun):** N/A — preview deployment, disposable.

## Leanness review
- **RIGOR:** basic
- **Findings:** Lean already. Ship.
- **Net removable:** 0
- **Disposition:** advisory-only
- **Raw report:** "Lean already. Ship." — all elements mandated by frozen architecture
  (A6 chain, ADR-0005 dispatch, S2 CORS shape); zero findings on all five tags.

## Readability review
- **Mode:** blocking
- **RIGOR:** basic
- **Findings:** Readable. Ship.
- **Unauthorized budget breaches:** 0
- **Authorized exemptions:** none
- **Disposition:** VERDICT: PASS (Decomposition/Budget/Justification all PASS)
- **Raw report:** largest file 87 lines; longest function 12 lines; nesting max 3 (at
  limit); one named function per concern with thin chain composer.

## Open / next session
- F-02 must: install gcov2lcov (pinned) in CI; use explicit deploy targets; account for
  Vercel SSO on preview URLs (production domain is the public smoke surface); consider
  test-verifier's content-hash-lock recommendation.
- test-verifier RUBRIC_GAPs recorded: pin-equality (runtime vs go.mod) owned by F-02 CI;
  static-route header behavior at the Vercel edge owned by F-13 deployed smoke.
