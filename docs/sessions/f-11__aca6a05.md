# Session: F-11
Date: 2026-08-07T07:30:00-05:00
Agent: subagent (test-author: general-purpose; builder: agents:golanger ×2 rounds; visual smoke: coordinator via real Chrome)
Piece / Brief: F-11 (docs/features/11-web-ui.md)
Baseline SHA -> Head: aca6a05 -> (merge commit of feature/f-11)

## Accomplished
- Full SPA replacing the F-01 placeholder: 81-input grid (gap-as-gridlines), tier-grouped
  seed dropdown, paste/manual/Clear, solve with status+grade+quartet, ladder-ordered
  histogram, step-through viewer (three distinct highlight states), full transport with
  Play/Pause auto-stop, clickable event log, 13-technique explanation panel + band chip,
  static hint, a11y floor. Zero inline code; textContent/createElement only;
  solution field never read (grep-proven).
- Visual smoke 11/11 in a real browser (both pre- and post-refactor sessions recorded).

## Decisions made (and why)
- Gridline recipe: 1px gaps + border-color background + 2px nth-child box boundaries
  (reads as contiguous 3px; keeps row-major DOM order).
- renderStep decomposed (round 2, readability-gate routed) into
  paintGrid/applyHighlights/renderPanels/syncLog — sequencer cc 1, parts ≤7.
- File-length exemption recorded (528>400): frozen 3-filename allow-list makes splits
  structurally unavailable; 76 lines contract-pinned data tables; ARCHITECTURE Known
  Tradeoffs concedes the vanilla-JS cost (D-031; readability r2 verified the exemption
  adversarially — "not a fig leaf").

## Deviations from the frozen plan
- none

## Test evidence
- Red-state: 51 DOM-contract assertions red vs placeholder (11/15 test functions);
  CSP/no-solver tests green-by-design (no false positives, proven).
- Green-state: repo-wide -race -count=1 green through both rounds.
- test-verifier: PASS (tuned; 4/4 mutations; AC-5 grep-proven; tokens verbatim) →
  scoped re-check after decomposition: PASS (suite + spot-mutation re-kill + functional
  solve; RUBRIC_GAP-1 staleness closed by the post-refactor re-smoke above).
- Readability: round 1 FAIL (renderStep cc~13; file 513>400 unauthorized) → routed per
  5b.9.3 (decompose + authorize) → round 2 PASS (33 functions measured; exemption
  survives adversarial tests).
- Leanness: 1 delete (.log-empty, applied) + micro cleanups (applied in round 2);
  otherwise clean.
- Compliance evidence: (no compliance ACs — hats N/A)

## Deployment evidence
- **Target:** manual (cicd_deploy_hook: manual) — deployment: SKIP
- **Rollback command (unrun):** N/A — assets ship in the binary; deploy at F-13

## Leanness review
- **RIGOR:** basic · **Findings:** applied (dead rule + 4 micro) · **Disposition:**
  follow-up folded into readability round; re-gated

## Readability review
- **Mode:** blocking · **RIGOR:** basic · **Findings:** r1 FAIL → decompose + recorded
  exemption → r2 PASS ("Readable. Ship."; 1 cosmetic advisory: stray log-panel class
  token) · **Disposition:** VERDICT: PASS r2

## Open / next session
- F-13: deploy through the gate; deployed UI smoke on the production domain.
