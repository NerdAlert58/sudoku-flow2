# Session: F-13
Date: 2026-08-07T09:30:00-05:00
Agent: coordinator (deploy-and-verify piece; no builders)
Piece / Brief: F-13 (docs/features/13-ship.md)
Baseline SHA -> Head: de03e29 (+PR16 deploy-path amendment)

## Accomplished
- PRODUCTION DEPLOYED through the manual gate: run 31186845829 (workflow_dispatch from
  master, environment `production`, operator approval recorded, remote-build
  `vercel deploy --prod`), workflow smoke green (health + UI + frozen headers, bounded
  retries). Live at https://sudoku-flow2.vercel.app
- Independent verification: /v1/health 200 {ok, go1.26.5(=go.mod pin), "1"}; GET / 200
  text/html; ALL four frozen headers verbatim on both (SECURITY F-12 CLOSED on the live
  surface); live solve (ORIGINAL#1 → solved/Easy/54=54/0.064ms, complete solution);
  catalog 4×(25/10/10/10); live generate hard→Hard 81 chars under the serverless clock.
- Deployed UI visual smoke (real Chrome): Very Hard #1 → solved · Expert, 66/66 events,
  54,924 checks, 0.85 ms; histogram shows the upper ladder live (pointing×5, claiming,
  naked_subset×3, w_wing×1); transport at 66/66; screenshots captured.

## Decisions made (and why)
- Deploy attempt 1 failed: stored VERCEL_TOKEN had expired — Vercel CLI now issues
  short-lived vca_ OAuth tokens (refreshToken+expiresAt); the secret's copy went stale
  (D-032). Fix: re-set the secret within the validity window per deploy. Note: the
  self-expiring credential IMPROVES the D-028 blast-radius posture.
- Deploy attempt 2 failed: `vercel deploy --prebuilt` cannot ship the classic Go
  runtime's locally-built output (ENOENT bootstrap on upload). Fix: F-02 amendment
  PR#16 — remote-build path (plain `vercel deploy --prod --yes`), the F-01-spike-proven
  route. Attempt 3 succeeded end-to-end.

## Deviations from the frozen plan
- deploy.yml amendment rode chore/f02-deploy-remote-build through full CI gates (PR16)
  — the amendment path, not a silent edit.

## Test evidence
- test-coverage gate: skipped (deploy-and-verify piece; no source, no tests — evidence
  is live runs and the recorded verification above)
- CI at deployed commit: master green (protection-gated merges only).
- Compliance evidence: (no compliance ACs — hats N/A)

## Deployment evidence
- **Target:** production (via gated workflow — the ONLY path; Vercel git integration
  disconnected since D-029)
- **Deploy command run:** vercel pull → vercel deploy --prod --yes (remote build)
- **Deploy exit code:** 0 (run 31186845829; attempts 31186633972/31186845829-1 failed
  as recorded above — no partial deploy went live from either)
- **Health check:** GET https://sudoku-flow2.vercel.app/v1/health → 200
  {"status":"ok","goVersion":"go1.26.5","apiVersion":"1"} — PASS (+ headers verbatim)
- **Rollback command (unrun):** vercel rollback <prior-deployment-url> — never
  auto-executed; human-owned
- **Env vars propagated:** none required at runtime (by design)
- **Deviations:** two failed attempts diagnosed and fixed (token expiry; prebuilt/Go
  incompatibility) — both recorded above

## Integration acceptance closeout (AC-6)
| UC | Instrument | Evidence |
|---|---|---|
| UC-1 | 55-corpus handler test, oracle-equal | F-10 gates + CI green at de03e29 |
| UC-2 | Replay proof 55/55 + 20 generated; determinism | F-06/F-08 gates; CI green |
| UC-3 | 100-seed matrix + 3/band handler + live deployed generate | F-08/F-10 gates + live hard→Hard above |
| UC-4 | Full-corpus batch under -race, ADR-0014 values | F-10 gates; CI green |
| UC-5 | -race every CI run + committed 10.8x negative benchmark | F-12 (docs/bench/scan-parallel.md) |
| UC-6 | Catalog pkg+handler+drift + live deployed sections | F-09/F-10 gates + live check above |
| UI | DOM contract 15/15 + visual smoke 11/11 + deployed smoke | F-11 + this session |
| Deployed health | Workflow smoke + independent curl + browser | This session |

## Leanness / Readability
- skipped (no source diff)

## Open / next session
- Token lifecycle: each future gated deploy needs a fresh VERCEL_TOKEN secret set within
  its validity window (or a dashboard-created long-lived token if the human prefers).
