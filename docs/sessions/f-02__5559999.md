# Session: F-02
Date: 2026-08-06T18:55:00-05:00
Agent: subagent (builder: general-purpose; pre-gate piece — no test-author)
Piece / Brief: F-02 (docs/features/02-ci-cd.md)
Baseline SHA -> Head: 5559999 -> 9a93884 (merge of PR #4)

## Accomplished
- ci.yml: 4 gate jobs (lint/build/test+coverage step/security-scan), SHA-pinned actions
  (checkout v7.0.1, setup-go v7.0.0), gcov2lcov@v1.1.1 → coverage.lcov, float-safe 80.0
  floor, govulncheck@v1.6.0 plain-text, contents:read.
- deploy.yml: workflow_dispatch-only, environment: production, explicit --prod targets
  (ADR-0016), production-domain smoke with frozen-header assertions (SECURITY F-12
  re-asserted every deploy).
- Operator infrastructure: repo PUBLIC (D-007), production environment with NerdAlert58
  as required reviewer (self-review allowed), 3 environment-scoped secrets (D-028
  provenance), branch protection strict + enforce_admins with required checks
  lint/build/test/security-scan.

## Decisions made (and why)
- Gate naming: vet runs as job `lint`; coverage is a named step inside `test` — required
  checks are the four job names (recorded in brief notes; ARCHITECTURE's five gate names
  map onto 4 jobs + 1 step).
- Go pin bumped 1.24.1 → 1.26.5 on this branch (cross-cutting fix, D-029): govulncheck
  v1.6.0 requires go ≥1.25 and 1.24.1 was no longer current (SECURITY F-11).
- Vercel git integration DISCONNECTED after it auto-connected on the public flip (D-029)
  — auto-deploy would bypass the manual gate (ADR-0010). No production deploy occurred
  outside the gate.

## Deviations from the frozen plan
- go.mod edit rode this branch outside F-02's allow-list — coordinator-authorized,
  logged (D-029); root cause was F-02's own gate discovery.

## Test evidence
- test-coverage gate: skipped (pre-gate piece: CI wiring; evidence is live runs)
- Live evidence: CI run 31131852753 (PR #4) — all 4 gates PASS including the coverage
  step (85.4% ≥ 80.0) and plain-text govulncheck (no vulnerabilities under go1.26.5).
- Gate-pause evidence (AC-5): dispatched run 31131992988 observably entered `waiting`
  for production approval and was deliberately REJECTED (comment recorded) — completed
  as failure with zero deploys.
- Compliance evidence: (no compliance ACs — hats N/A)

## Deployment evidence
- **Target:** manual (cicd_deploy_hook: manual — deploys only via the gated workflow)
- deployment: SKIP (cicd_deploy_hook: manual); the gate mechanics themselves were
  exercised and rejected deliberately (see above). Real deploy at F-13.
- **Rollback command (unrun):** vercel rollback ${PRIOR_DEPLOYMENT_URL}

## Leanness review
- skipped (pre-gate piece: YAML wiring, no source diff)

## Readability review
- **Mode:** blocking — skipped (pre-gate piece: Test command N/A)

## Open / next session
- F-13 confirms sudoku-flow2.vercel.app is the production alias on first gated deploy.
- Required-check names frozen as lint/build/test/security-scan; any workflow job rename
  must update protection in the same change.
