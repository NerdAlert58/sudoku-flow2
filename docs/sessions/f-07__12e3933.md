# Session: F-07
Date: 2026-08-07T02:00:00-05:00
Agent: subagent (test-author owns the piece end-to-end — test-only)
Piece / Brief: F-07 (docs/features/07-technique-fixtures.md)
Baseline SHA -> Head: 12e3933 -> (merge commit of feature/f-07)

## Accomplished
- Ladder-cap harness (test-only SolveCapped, line-faithful to Solve's loop, byte-compare
  verified) + registry-drift guard.
- 13/13 fires-and-sound through oracle.ReplayVerify — the committed positive
  per-checker coverage F-06's gate demanded (incl. hidden_subset + jellyfish).
- Necessity+sufficiency PROVEN for 9/13 techniques; 4/13 (hidden_subset, swordfish,
  jellyfish, xyz_wing) on the PRD's recorded-evidence fallback —
  solver/testdata/techniques/EVIDENCE.md records real searches (3,190-state pool,
  44,660 capped solves, beam/hill-climb, 75k-200k node budgets; the search genuinely
  upgraded claiming to proven). Tripwire test pins fallback count at 4 and fails
  loudly if a future ladder change unlocks one.
- Six two-completion fires states additionally prove opening eliminations by exact
  brute force (covers ReplayVerify's documented oracle-skip on non-unique states —
  verifier confirmed single-event streams there, so coverage is total).

## Decisions made (and why)
- Fires fixtures reuse committed F-04/F-05 grids (same test package) — no duplication.
- EVIDENCE.md lives in testdata (PRD's "evidence recorded" requirement, not prose docs).

## Deviations from the frozen plan
- none

## Test evidence
- Red-state: N/A by design (test-only piece; fixtures verify the built solver — any red
  would be a surfaced solver defect; none surfaced).
- Green-state: repo-wide -race -count=1 green (author + verifier ×2).
- test-verifier: PASS round 1 (tuned; 3 mutations — 2 killed by named assertions, 1
  survivor logged as RUBRIC_GAP-1: SolveCapped's unsolvable branch undefended →
  FOLLOWUPS; independent oracle survey + AC-3 capped re-runs performed).
- Compliance evidence: (no compliance ACs — hats N/A)

## Deployment evidence
- **Target:** manual (cicd_deploy_hook: manual) — deployment: SKIP
- **Rollback command (unrun):** N/A — test-only piece

## Leanness review
- skipped (test-only piece: zero changed source lines)

## Readability review
- **Mode:** blocking — skipped (zero changed source lines)

## Open / next session
- FOLLOWUPS: SolveCapped zero-candidate self-test (RUBRIC_GAP-1).
- F-13 consumes the per-technique status table as PRD success-criteria evidence.
